# Fluor.js

## Installation

```shell
$ npm install fluor
```

or

```shell
$ yarn add fluor
```

## Development

Commands:

- `build`: shortcut for `build:dev`
- `build:prod`: minify all the things
- `build:dev`: the dev version
